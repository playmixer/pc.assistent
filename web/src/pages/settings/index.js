import React from "react";
import LayoutPage from "../../components/layoutPage";


const PageSettings = ({back}) => {

    return <LayoutPage back={back} title={"Settings"}>
        Settings
    </LayoutPage>
}

export default PageSettings;